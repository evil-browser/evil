# Theme

`evil-dark` is a Chromium theme extension. Palette and the reasoning behind it
are in [../docs/THEME.md](../docs/THEME.md).

Load it during development with `../scripts/dev-run.sh`; pack it for release
with `../tools/pack-extensions.sh`.
